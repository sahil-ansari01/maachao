import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"
import { Suspense } from "react"

export const metadata: Metadata = {
  title: "Food Delivery App - Order Cakes & Desserts",
  description:
    "Browse and order delicious cakes, desserts, and treats with fast delivery. Get exclusive deals and discounts on your favorite items.",
  generator: "v0.app",
  keywords: ["food delivery", "cakes", "desserts", "online ordering", "quick delivery"],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable} antialiased`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
